name: "🔬 PR: test"

on:
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: 🧺 checkout
        uses: actions/checkout@v3
      - name: 📌 pnpm install
        run: pnpm install
      - name: 🕵️ pnpm test
        run: pnpm test
