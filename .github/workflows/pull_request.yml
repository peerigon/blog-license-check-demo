name: "🔬 PR: test"

on:
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest
    needs: [no-draft]
    steps:
      - name: 🧺 checkout
        uses: actions/checkout@v3
      - name: 📌 npm install
        run: npm ci --prefer-offline --no-audit
      - name: 🕵️ npm test
        run: npm test
